---
import { ctas } from "../site";
import WhatsAppCta from "./WhatsAppCta.astro";

interface Props {
  /** If true, renders the sticky bar at all. Set false on pages that already
   *  end in a full-width booking block (e.g. /fractional-cmo after the Calendly embed). */
  enabled?: boolean;
  /** The pixel threshold (scrollY) after which the bar appears. */
  revealAfter?: number;
  /** Contextual override, e.g. "Enquire about speaking" on /speaking. */
  label?: string;
  href?: string;
  hint?: string;
}

const {
  enabled = true,
  revealAfter = 1400,
  label = `${ctas.growthNote.label} →`,
  href = ctas.growthNote.href,
  hint = ctas.growthNote.hint
} = Astro.props;

// The bar always ends in an equal-weight pair: newsletter | WhatsApp group.
// Some pages override the CTA with something else (a speaking enquiry, a
// download); that override rides above the pair on its own row. When the page
// CTA *is* the newsletter — the default — the pair carries it, so we never
// show two links to the same destination.
const showPageCta = href !== ctas.growthNote.href;
---

{enabled && (
  <div
    class="sticky-cta"
    data-reveal-after={revealAfter}
    role="complementary"
    inert
  >
    <div class="sticky-cta-inner">
      <div class="sticky-cta-copy">
        <span class="sticky-cta-hint">{hint}</span>
      </div>
      <button
        class="sticky-cta-dismiss"
        type="button"
        aria-label="Dismiss newsletter banner"
      >
        ×
      </button>
      <div class="sticky-cta-actions">
        {showPageCta && (
          <a
            class="sticky-cta-btn sticky-cta-lead"
            href={href}
            data-cta="sticky-page-cta"
          >
            {label}
          </a>
        )}
        <div class="sticky-cta-duo">
          <a
            class="sticky-cta-btn sticky-cta-btn-newsletter"
            href={ctas.growthNote.href}
            data-cta="sticky-newsletter"
          >
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false">
              <rect x="3" y="5" width="18" height="14" rx="2"></rect>
              <path d="m3 7 9 6 9-6"></path>
            </svg>
            {ctas.growthNote.label}
          </a>
          <WhatsAppCta
            variant="button"
            buttonClass="sticky-cta-btn sticky-cta-btn-whatsapp"
            label="Join the WhatsApp group"
            dataCta="sticky-whatsapp"
          />
        </div>
      </div>
    </div>
  </div>
)}

<style is:global>
  @import "../styles/mobile-qa.css";

  .sticky-cta {
    position: fixed;
    inset-inline: 0;
    bottom: max(1.25rem, env(safe-area-inset-bottom, 0px));
    z-index: 45;
    display: flex;
    justify-content: center;
    padding: 0 1rem;
    pointer-events: none;
    opacity: 0;
    transform: translateY(120%);
    transition: opacity 280ms ease, transform 320ms cubic-bezier(.2,.9,.3,1);
  }

  .sticky-cta.is-visible {
    opacity: 1;
    transform: translateY(0);
    pointer-events: auto;
  }

  .sticky-cta.is-dismissed {
    display: none;
  }

  /* Two rows: the hint (with the dismiss control) above a row of labelled
     CTAs, so both the newsletter and the WhatsApp group read as real,
     clickable offers rather than mystery icons. */
  .sticky-cta-inner {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    grid-template-areas:
      "copy dismiss"
      "actions actions";
    align-items: center;
    gap: 0.55rem 0.65rem;
    width: min(640px, 100%);
    padding: 0.7rem 0.7rem 0.75rem 1rem;
    border-radius: 0.75rem;
    background: color-mix(in srgb, var(--ink) 94%, transparent);
    color: #fff;
    box-shadow: 0 10px 28px -16px color-mix(in srgb, var(--ink) 55%, transparent);
    backdrop-filter: saturate(140%) blur(6px);
  }

  .sticky-cta-copy {
    grid-area: copy;
    display: flex;
    align-items: center;
    line-height: 1.35;
    min-width: 0;
  }

  .sticky-cta-hint {
    font-size: 0.8rem;
    font-weight: 500;
    letter-spacing: -0.005em;
    color: color-mix(in srgb, #fff 82%, transparent);
  }

  .sticky-cta-actions {
    grid-area: actions;
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }

  /* The newsletter and the group get identical space and identical weight —
     neither is the junior partner. */
  .sticky-cta-duo {
    display: flex;
    align-items: stretch;
    gap: 0.4rem;
  }

  .sticky-cta-duo > * {
    flex: 1 1 0;
    min-width: 0;
  }

  .sticky-cta-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.45rem;
    min-height: 2.4rem;
    padding-block: 0.5rem;
    padding-inline: 0.85rem;
    border-radius: 0.5rem;
    font-size: 0.82rem;
    font-weight: 600;
    line-height: 1.25;
    text-align: center;
    transition: background 150ms ease, border-color 150ms ease;
  }

  .sticky-cta-btn svg {
    width: 1rem;
    height: 1rem;
    flex-shrink: 0;
  }

  /* Identical treatment for both halves — same size, same colour, same
     weight. Anything else makes one of them the junior partner. Solid white
     on the near-black bar is the highest-contrast option available. */
  .sticky-cta-btn-newsletter,
  .sticky-cta-btn-whatsapp {
    border: 1px solid #fff;
    background: #fff;
    color: var(--ink);
  }

  .sticky-cta-btn-newsletter:hover,
  .sticky-cta-btn-newsletter:focus-visible,
  .sticky-cta-btn-whatsapp:hover,
  .sticky-cta-btn-whatsapp:focus-visible {
    background: color-mix(in srgb, #fff 88%, var(--ink));
    border-color: color-mix(in srgb, #fff 88%, var(--ink));
    outline: none;
  }

  /* Page-specific CTA (speaking enquiry, media kit) rides above the pair,
     in the brand accent so it still leads where a page has one. */
  .sticky-cta-lead {
    width: 100%;
    border: 1px solid var(--accent);
    background: var(--accent);
    color: #fff;
  }

  .sticky-cta-lead:hover,
  .sticky-cta-lead:focus-visible {
    background: color-mix(in srgb, var(--accent) 85%, white);
    border-color: color-mix(in srgb, var(--accent) 85%, white);
    outline: none;
  }

  .sticky-cta-dismiss {
    grid-area: dismiss;
    flex-shrink: 0;
    width: 1.9rem;
    height: 1.9rem;
    border-radius: 999px;
    border: 1px solid color-mix(in srgb, #fff 18%, transparent);
    background: transparent;
    color: #fff;
    font-size: 1rem;
    line-height: 1;
    cursor: pointer;
    transition: background 150ms ease;
  }

  .sticky-cta-dismiss:hover,
  .sticky-cta-dismiss:focus-visible {
    background: color-mix(in srgb, #fff 14%, transparent);
    outline: none;
  }

  @media (max-width: 520px) {
    .sticky-cta {
      bottom: 0.75rem;
    }
    .sticky-cta-inner {
      gap: 0.5rem;
      padding: 0.7rem;
      border-radius: 1.25rem;
    }
    .sticky-cta-hint {
      font-size: 0.74rem;
      line-height: 1.4;
    }
    /* Stacked below 520px — still equal-sized, just full-width targets
       rather than two cramped columns. */
    .sticky-cta-duo {
      flex-direction: column;
    }
    .sticky-cta-btn {
      min-height: 2.6rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .sticky-cta {
      transition: none;
    }
  }
</style>

<script is:inline>
  (function () {
    var bar = document.querySelector('.sticky-cta');
    if (!bar) return;

    var revealAfter = Number(bar.getAttribute('data-reveal-after') || 1400);
    var dismissKey = 'dj-sticky-cta-dismissed';
    var dismissed = false;
    var overlapSelector = 'footer, .site-footer-v2, form, .growth-diagnosis, .cta-gradient, .evergreen-updated, #proof, [aria-label="Ways to work together"], .v5-ladder, .v5-final-cta, .v5-section-proof';
    var overlapsSensitiveArea = false;

    try {
      dismissed = sessionStorage.getItem(dismissKey) === '1';
    } catch (err) {
      // sessionStorage may be blocked (Safari private). Fall back to in-memory.
    }

    if (dismissed) {
      bar.classList.add('is-dismissed');
      return;
    }

    function update() {
      if (dismissed) return;
      var shouldShow = window.scrollY > revealAfter && !overlapsSensitiveArea;
      bar.classList.toggle('is-visible', shouldShow);
      bar.toggleAttribute('inert', !shouldShow);
      // Reserve space at the bottom of the page while the bar is shown so it
      // never sits on top of body text or cards.
      document.body.style.paddingBottom = shouldShow ? '5.5rem' : '';
    }

    var dismissBtn = bar.querySelector('.sticky-cta-dismiss');
    if (dismissBtn) {
      dismissBtn.addEventListener('click', function () {
        dismissed = true;
        bar.classList.remove('is-visible');
        bar.setAttribute('inert', '');
        bar.classList.add('is-dismissed');
        try {
          sessionStorage.setItem(dismissKey, '1');
        } catch (err) {
          // ignore
        }
      });
    }

    if ('IntersectionObserver' in window) {
      var observer = new IntersectionObserver(function (entries) {
        overlapsSensitiveArea = entries.some(function (entry) {
          return entry.isIntersecting;
        });
        update();
      }, {
        rootMargin: '40px 0px 40px 0px',
        threshold: [0, 0.05, 0.1]
      });

      document.querySelectorAll(overlapSelector).forEach(function (el) {
        observer.observe(el);
      });
    }

    window.addEventListener('scroll', update, { passive: true });
    window.addEventListener('resize', update, { passive: true });
    // Run once on load so it's correct after a deep-link / back-nav.
    update();
  })();
</script>
